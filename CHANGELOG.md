# Changelog

BillHub（原 WZBill）——合同 / 发票 / 审批 / 报销一体化处理工具。
桌面版（PyQt6）与 Web 版（Flask）共享同一套数据与报表逻辑。

版本格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
并遵守 [语义化版本](https://semver.org/lang/zh-CN/)。

## [2.0.0] - 2026-08-13

### 新增

- 合同信息增加「生效时间」「结束时间」字段：表单录入、详情页展示、清单导入导出（含别名兼容：生效日期/开始时间、结束日期/到期时间）

### 变更

- **完全 Web 化 + 全新现代化 UI**：重构为深色侧边栏 + 顶栏 + 卡片式布局（参考设计稿），导航分「工作台 / 业务管理 / 系统管理」；登录页、合同清单、合同详情、报销管理、用户管理全部改版；报销填报与审批表预览保留弹窗交互；版本号升至 2.0.0
- 路由重构：`/contracts` 合同清单、`/contracts/<id>` 合同详情独立页面、`/payments` 报销管理页、`/` 工作台汇总

## [1.2.2] - 2026-08-13

### 新增

- 经办人（归属用户）支持管理员修改：合同表单增加用户下拉（新建默认自己、编辑默认当前归属），合同列表新增「经办人」列（仅管理员可见）
- 重复启动服务保护：端口被占用时清晰提示已有服务地址并立即退出，不再进入空转重启循环（热重载子进程跳过检查避免误报）

### 变更

- 顶栏按钮整理为下拉菜单：「管理 ▾」（用户管理 / 备份数据库，仅管理员）+「用户名 ▾」（修改密码 / 退出登录）；普通用户顶栏仅 1 个按钮
- 术语统一：「客户/甲方」→「签订单位」（Web 全部页面 + 桌面版 + 导入导出表头；预览与审批表模板措辞对齐）
- 经办人=当前登录用户：表单不再手填经办人，新建自动取当前用户显示名；编辑保留原值
- 付款计划去掉「条件」字段：编辑器为 期 / 比例 / 金额 三列（Web + 桌面版），详情与阶段下拉同步去除；数据库列保留兼容老数据
- 合同分类字段由 datalist 改为真正的下拉选择框（预设 5 类 + 未分类，导入的自定义值保留回显）

### 修复

- 合同列表显示错乱：静态资源加启动时间戳缓存破坏（`?v=`），解决浏览器缓存旧版 CSS 导致的新版页面渲染异常；移除表格 `overflow:hidden`（与 sticky 表头冲突）
- Docker 镜像构建失败：`opencv-python`（GUI 版需 X11 库）替换为 `opencv-python-headless`，slim 镜像可正常导入 rapidocr
- 数据库同日多次备份重名失败：备份文件名加毫秒时间戳

## [1.2.1] - 2026-08-13

### 变更

- **Web 界面重构**：合同列表铺满整页（数据表格：编号/名称/签订单位/总额/已付/剩余/付款进度条/操作）
- 报销录入改为弹窗（OCR 拖拽上传 + 表单 + 阶段下拉联动）
- 审批表预览改为弹窗（打印 / 下载 Excel；生成后自动弹出）
- 合同详情改为表格行内展开（信息卡 + 付款计划 + 历史支付记录）

### 新增

- 付款进度条（蓝色渐变，100% 变绿）
- 弹窗系统：遮罩点击关闭、ESC 关最上层、body 滚动锁定、双层堆叠（预览浮于报销之上）
- 打印优化：仅输出审批表正文（`@media print` 重置弹窗定位）

## [1.2.0] - 2026-08-12

### 新增

- **品牌更名**：WZBill → BillHub，仓库转公开（脱敏内部邮箱）
- **Web 版上线**（Flask + Jinja2 + HTMX，内网多人共享）：
  - P1 地基：应用工厂 + 本地账号登录 + 用户表/owner 字段迁移 + SQLite WAL
  - P2 合同管理：列表/搜索/详情/增删改/付款计划，按经办人隔离（管理员全局）+ Excel 导入导出
  - P3 报销填报：OCR 发票识别（图片/PDF 拖拽上传）+ 生成审批表 xlsx + 历史记录 + 发票号重复校验
  - P4 审批表预览：HTML 仿模板排版 + 浏览器打印 + 下载 Excel
  - P5 权限：用户管理（增删改/重置密码/防自删/防删最后管理员）、修改自己密码、数据库一键备份、LDAP/AD 双通道登录
- **CI/CD 双产物**（`.github/workflows/build-release.yml`）：
  - 桌面版 `BillHub.exe`（windows-latest PyInstaller）
  - Web 版 Docker 镜像 `billhub-web`（导出离线 tar，zstd 压缩，内网 `docker load`）
  - 统一 GitHub Release 发版（版本号取自 pyproject.toml）
- 部署套件：`Dockerfile.web`、`docker-compose.yml`、`.env.example`、`deploy.md`（离线内网部署/升级/备份指南）
- 共享工具模块 `utils.py`（金额大写 / 文件名清洗 / 审批表上下文，桌面与 Web 共用）

### 变更

- `DB_PATH` 支持环境变量（`BILLHUB_DB`），桌面版默认行为不变
- 版本号统一为单一来源（pyproject.toml）

### 移除

- 邮件分发暂停（脚本保留，工作流中禁用待稳定后恢复）

## [1.1.0] - 2026-08-10

### 新增

- CI/CD：push 自动打包 exe，7-Zip 分卷（≤30MB）邮件分发
- GitHub Release 自动发布

### 变更

- 发票开票日期字段（OCR 自动填充，报销日期默认今天）
- 已报销期数在阶段选择器中自动置灰跳过

### 修复

- 打包后 OCR 不可用：rapidocr 模型/config 通过 collect_data_files 打包

## [1.0.0] - 2026-08-08

### 新增

- 桌面版 WZBill（PyQt6）：合同管理（付款计划/分类/收款信息）、OCR 发票识别（图片/PDF）、模板驱动审批表生成（统一归档）、本地 SQLite 存储
